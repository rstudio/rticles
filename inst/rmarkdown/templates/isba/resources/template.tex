% based on https://vtex-soft.github.io/texsupport.isba-ba/
% \documentclass[ba,preprint]{imsart}% use this for supplement article
\documentclass[ba]{imsart}

\pubyear{2021}
\volume{TBA}
\issue{TBA}
% \doi{0000}
\arxiv{2010.00000}
\firstpage{1}
\lastpage{1}

%
\usepackage{amsthm}
\usepackage{amsmath}
\usepackage{natbib}
\usepackage[colorlinks,citecolor=blue,urlcolor=blue,filecolor=blue,backref=page]{hyperref}
\usepackage{graphicx}

\startlocaldefs
\numberwithin{equation}{section}
\theoremstyle{plain}
\newtheorem{thm}{Theorem}[section]
\endlocaldefs

% Pandoc header
$for(header-includes)$
$header-includes$
$endfor$

$preamble$

\begin{document}
\begin{frontmatter}

\title{$title$}
\runtitle{$title$}

\begin{aug}
$for(author/pairs)$
\author{$author.value.firstname$ $author.value.lastname$\thanksref{$author.value.affiliationref$,$if(author.value.footnoterefs)$$for(author.value.footnoterefs)$$it$$sep$,$endfor$}$endif$
$if(author.value.email)$\ead[label=ea-$author.value.affiliationref$-$author.key$,email]{$author.value.email$}$endif$
$if(author.value.url)$\ead[label=ua-$author.value.affiliationref$-$author.key$,url]{$author.value.url$}$endif$}$sep$, $endfor$

\runauthor{$for(author/first)$$it.lastname$$endfor$$if(author/rest/rest)$ et al.$else$$for(author/rest)$ \and $author.lastname$$endfor$$endif$}

$for(affiliations)$ %loop through affiliations
\address[$affiliations.ref$]{$affiliations.name$,$affiliations.address$ $for(author/pairs)$
$if(it.value.email)$
\ifstrequal{$affiliations.ref$}{$it.value.affiliationref$}{\printead{ea-$affiliations.ref$-$it.key$}}{}
$endif$
$if(it.value.url)$
\ifstrequal{$affiliations.ref$}{$it.value.affiliationref$}{\printead{ua-$affiliations.ref$-$it.key$}}{}
$endif$
$sep$ $endfor$%authors
}$endfor$%affiliations

$for(footnotes)$
\thankstext{$it.ref$}{$it.text$}
$endfor$

\end{aug}

\begin{abstract}
$abstract$
\end{abstract}

% MSC class
$if(MSC2020primary)$ \begin{keyword}[class=MSC]
$for(MSC2020primary/first)$\kwd[Primary ]{$it$}$endfor$
$if(MSC2020primary/rest)$$for(MSC2020primary/rest)$\kwd{$it$}$sep $$endfor$$endif$
$if(MSC2020secondary)$$for(MSC2020secondary/first)$\kwd[; secondary ]{$it$}$endfor$
$if(MSC2020secondary/rest)$$for(MSC2020secondary/rest)$\kwd{$it$}$sep $$endfor$$endif$$endif$\end{keyword}$endif$
% keywords
$if(keywords)$ \begin{keyword} $for(keywords)$\kwd{$keywords$}$sep$ $endfor$\end{keyword}$endif$
\end{frontmatter}

$body$

$for(include-after)$
$include-after$
$endfor$

$if(supplements)$
$for(supplements)$
\begin{supplement}
\stitle{$it.title$}
\sdescription{$it.description$}
\end{supplement}
$endfor$$endif$

$if(acknowledgements)$
\begin{acks}[Acknowledgments]
$acknowledgements$
\end{acks}
$endif$

\bibliographystyle{ba}
\bibliography{$bibliography$}

\end{document}

