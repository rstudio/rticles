\documentclass[times]{simauth}
\usepackage{moreverb}
\usepackage[T1,mtbold]{mathtime}
\usepackage[colorlinks,bookmarksopen,bookmarksnumbered,citecolor=red,urlcolor=red]{hyperref}
\usepackage[numbers, sort&compress]{natbib}
\def\volumeyear{$year$}

\begin{document}

\runninghead{$authabbr$}

\title{$title$}
\author{$author$}
\address{$for(address)$\affilnum{$address.num$} $address.address$\\$endfor$}
\corraddr{$corraddr$}

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$if(keywords)$
\keywords{$keywords$}
$endif$

\maketitle

$body$

$if(natbib)$
\bibliographystyle{wileyj}
\bibliography{$bibliography$}
$endif$

$for(include-after)$
$include-after$

$endfor$
\end{document}
