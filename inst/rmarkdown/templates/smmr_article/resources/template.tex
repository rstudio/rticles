\documentclass[$for(classoption)$$classoption$,$endfor$]{sagej}

\usepackage{moreverb,url,natbib}
\usepackage[colorlinks,bookmarksopen,bookmarksnumbered,citecolor=red,urlcolor=red]{hyperref}

$if(longtable)$
\usepackage{longtable}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

%\textsc{\raggedbottom
%
%\providecommand{\tightlist}{%
%  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}\textsc{}}

\begin{document}

\title{$title$}

\runninghead{$runninghead$}

$if(author)$
\author{$author$}
$endif$

$if(corrauth)$
\corrauth{$corrauth$}
$endif$

$if(email)$
\email{$email$}
$endif$

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$if(keywords)$
\keywords{$keywords$}
$endif$

\maketitle

$body$

$if(bibliography)$
\bibliographystyle{$bibliographystyle$}
\bibliography{$bibliography$}
$endif$

$for(include-after)$
$include-after$

$endfor$

\end{document}
